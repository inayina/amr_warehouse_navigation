# AMR / SLAM 项目设计说明（V1 收束版）

## 1. 文档目的

本文件用于收束当前 AMR / SLAM 项目主线，避免在调试过程中不断扩散需求、混入历史试验逻辑，导致项目失控。

本文件的作用不是写项目背景材料，而是作为当前开发和排障的控制文档，用来约束：

- 当前版本目标
- 当前版本范围
- 当前允许修改的文件
- 当前排障顺序
- AI 辅助修改规则

---

## 2. 当前项目目标（V1）

当前版本只做 **AMR 仿真建图最小闭环**，目标如下：

1. Gazebo 中可以正常显示机器人
2. 机器人可以响应运动控制
3. `/scan` 话题正常发布
4. TF 链路正确连通
5. `slam_toolbox` 可以正常建图并输出 `/map`

当前版本完成后，即可作为简历和面试中的一个可讲项目。

---

## 3. 当前版本边界

### 3.1 当前版本包含的内容

- Gazebo Harmonic 仿真环境
- AMR 机器人模型加载
- 激光雷达传感器数据发布
- `ros_gz_bridge` 桥接
- ROS 2 Jazzy 下的话题、TF、建图验证
- `slam_toolbox` 建图
- README / 设计说明 / 排障记录整理

### 3.2 当前版本明确不做的内容

以下内容不属于当前 V1 主线，不参与本轮开发与排障：

- Nav2 导航
- 自动路径跟踪
- CAD 地图转路径控制
- WMS / 调度系统接入
- Docker 完整部署方案
- 视觉障碍识别
- 多车协同
- 地图优化与高级算法改进
- 前端界面开发

这些内容保留为后续扩展方向，但本轮不进入主线。

---

## 4. 当前系统链路

当前主线链路如下：

`Gazebo World`
→ `Robot Model`
→ `Lidar Plugin`
→ `Gazebo Topic`
→ `ros_gz_bridge`
→ `ROS 2 /scan`
→ `TF Tree`
→ `slam_toolbox`
→ `ROS 2 /map`
→ `RViz / 建图验证`

说明：

- 当前排障必须围绕这条链路逐段验证
- 任何问题必须先定位断点所在层级
- 不允许在链路前段未确认之前直接修改后段配置

---

## 5. 当前已知状态

### 已确认内容

- 项目历史上做过窄道场景实验
- 项目历史上做过 Docker 相关尝试
- 当前主线已收束为 V1：Gazebo + AMR + `/cmd_vel` + `/scan` + TF + `slam_toolbox`
- 机器人已改为由 `worlds/warehouse_full.world` 直接 include `model://my_robot`
- 当前 `/scan`、`/odom`、TF 和 `/map` 均已打通
- `slam.launch.py` 会自动 configure / activate `slam_toolbox`

### 当前问题

- 键盘控制需要避免被旧的 `ros2 topic pub -r ... /cmd_vel` 抢占
- 旧 Nav2 / 旧 TF / 旧 launch 进程会污染当前 V1 运行图，调试前必须干净启动
- 当前建图链路可运行，但地图质量仍需要通过慢速、小闭环驾驶继续优化

### 当前判断

当前问题重点不再是模型加载或 topic 是否存在，而在：

1. 启动前是否清理旧进程
2. `/cmd_vel` 是否只有预期的控制来源
3. `slam_toolbox` 是否为 `active [3]`
4. 建图驾驶路线是否足够慢、稳，并形成小闭环

---

## 6. 当前主线目标拆分

### 模块 A：Gazebo 场景与模型显示

目标：

- Gazebo world 正常启动
- 机器人成功加载到仿真环境中
- 机器人在画面中可见
- 机器人不会掉地、穿模或异常漂移

验收标准：

- Gazebo 中能明确看到机器人实体
- Gazebo 实体树中存在机器人模型
- 启动日志无明显模型 include / URI 解析错误

---

### 模块 B：机器人运动控制

目标：

- 机器人能响应运动指令
- `/cmd_vel` 或对应控制链路有效

验收标准：

- 发送控制指令后机器人产生可观察运动
- 控制链路与插件配置一致
- 运动行为与底盘模型配置匹配

---

### 模块 C：激光雷达与扫描数据

目标：

- 激光雷达插件正常工作
- `/scan` 正常输出

验收标准：

- `ros2 topic list` 中存在 `/scan`
- `ros2 topic echo /scan` 有数据
- RViz 中 LaserScan 可显示

备注：

- 当前已知 `/scan` 已有数据
- 本轮要做的是保持其正常，不因其他改动被破坏

---

### 模块 D：TF 链路

目标：

- 建立并验证建图所需 TF 关系

当前期望链路：

`odom -> base_link -> my_robot/lidar_link/lidar`

验收标准：

- RViz Fixed Frame 设置正确时无明显报错
- `view_frames` 结果符合预期
- `/scan` 的 `frame_id` 与 TF 关系一致

---

### 模块 E：SLAM 建图

目标：

- `slam_toolbox` 正常启动
- 地图能够随机器人运动实时更新

验收标准：

- `/map` 存在
- RViz 中可见建图结果
- 可保存地图文件

---

## 7. 排障优先级（必须严格按顺序）

当前调试必须按以下顺序进行，不允许跳步：

### 第一步：确认机器人是否真正加载成功
检查内容：

- 启动日志是否有 `model://my_robot` 解析失败
- 模型路径是否正确
- world / launch 中引用是否正确
- Gazebo 实体树中是否存在 robot

### 第二步：确认机器人是否可见
检查内容：

- 初始位置是否异常
- 模型是否掉入地下
- 视角是否偏离
- 可视化 mesh / link 是否正确

### 第三步：确认机器人是否能运动
检查内容：

- `/cmd_vel` 是否发出
- Gazebo / bridge / 驱动插件是否匹配
- wheel/joint 配置是否正确

### 第四步：确认 `/scan` 持续正常
检查内容：

- `/scan` 是否仍存在
- `frame_id` 是否正确
- RViz 是否能正常显示

### 第五步：确认 TF 链路
检查内容：

- `base_link`、`lidar_link`、`odom` 等关系是否正确
- `robot_state_publisher` 是否正常

### 第六步：验证 slam_toolbox 建图
检查内容：

- `ros2 lifecycle get /slam_toolbox` 是否为 `active [3]`
- `slam_toolbox` 是否订阅 `/scan`
- `/map` 是否生成
- 地图是否随机器人运动更新
- 参数文件是否合理

### 第七步：优化建图质量
检查内容：

- 是否干净重启，避免旧进程污染
- 是否先做小范围闭环，而不是直接扫全仓库
- 车速是否过快，角速度是否过大
- 是否贴墙、贴货架过近

---

## 8. 当前目录治理原则

本项目已存在多个历史试验方向，包括但不限于：

- 窄道场景实验
- Docker 尝试
- 旧版 launch / world / config
- CAD 占位内容
- WMS 占位内容

这些内容不删除，但不应与当前主线混用。

### 目录治理原则

#### 当前主线目录
仅保留当前 V1 所需内容，例如：

- `launch/`
- `models/`
- `worlds/`
- `config/`
- `scripts/`
- `design.md`
- `README.md`

#### 历史试验或未来扩展目录
建议迁移到：

- `archive/`
- `future_extensions/`

建议分类示例：

- `archive/narrow_aisle_old/`
- `archive/old_launch/`
- `future_extensions/docker/`
- `future_extensions/cad_map_pipeline/`
- `future_extensions/wms_integration/`

## 8.1 当前主线与历史版本划分

为避免项目在调试过程中混入历史试验逻辑，现将项目中的内容划分为“当前主线”和“历史版本/扩展版本”。

### 当前主线（Current Mainline）

当前主线指本轮 V1 只认的一套开发路径，目标是完成 AMR 仿真建图最小闭环：

- Gazebo 中正常显示机器人
- 机器人可运动
- `/scan` 正常
- TF 正常
- `slam_toolbox` 建图成功

当前主线应只包含与上述目标直接相关的文件与配置。

### 历史版本（Archived Versions）

以下内容属于历史试验版本，不作为当前主线的一部分：

- 旧窄道场景实验版本
- 旧 launch / 旧 world / 旧 config
- 已不再作为当前调试入口的模型或脚本
- 临时测试文件
- 早期验证用的备份文件

这些内容保留，但统一归档到 `archive/`，避免与当前主线混用。

### 扩展版本（Future Extensions）

以下内容属于未来扩展方向，不属于当前 V1 主线：

- Docker 环境封装
- CAD 地图/路径相关尝试
- WMS / 调度系统接口
- 路径控制 / 导航扩展
- 更复杂仓储系统功能

这些内容建议归类到 `future_extensions/`，仅作为后续规划，不参与本轮排障。
---

## 9. 各扩展项当前定位

### 9.1 CAD
定位：后续扩展项

用途：

- 后续可作为地图来源
- 后续可用于路径点设计与场景还原

当前处理：

- 不删除
- 不参与本轮主线修改
- 从当前主线目录中隔离

---

### 9.2 WMS
定位：后续上层系统扩展项

用途：

- 后续任务下发
- 状态回传
- 仓储调度逻辑模拟

当前处理：

- 不删除
- 不参与当前 V1
- 仅保留为系统规划方向

---

### 9.3 Docker
定位：工程化增强项

用途：

- 后续环境复现
- 一键部署
- 降低项目运行门槛

当前处理：

- 不删除
- 不参与当前 V1 排障
- 仅在主功能稳定后再整理

---

## 10. 当前允许修改的文件范围

本轮只允许修改与主线问题直接相关的文件。

优先允许修改的范围示例：

- `launch/*.launch.py`
- `worlds/*.world`
- `models/my_robot/*`
- `config/bridge.yaml`
- `config/slam_toolbox*.yaml`
- 与 TF / 机器人显示 / 控制链路直接相关的少量文件

### 当前禁止进行的操作

- 不要大规模重构整个项目
- 不要同时重写多个模块
- 不要在本轮引入 Nav2 / WMS / CAD 路径控制 / Docker 完整方案
- 不要因为一个问题去重建整个工程
- 不要在未确认当前问题点前一次性改很多文件

---

## 11. AI 辅助修改规则

所有 AI 辅助修改必须遵守以下规则：

### 规则 1：一次只解决一个问题
当前轮次只允许处理一个明确问题，例如：

- 机器人不可见
- 机器人不可运动
- TF 异常
- slam_toolbox 无法建图

不得同时处理多个主问题。

### 规则 2：修改前必须先说明
在给出修改方案前，必须先明确：

1. 当前判断的问题点是什么
2. 准备修改哪些文件
3. 每个修改的目的是什么
4. 修改后预期会看到什么现象

### 规则 3：未经确认不得扩大范围
不得擅自：

- 新增复杂模块
- 修改无关目录
- 混入 archive / future_extensions 中的旧逻辑
- 把 V2 / V3 功能提前拉入主线

### 规则 4：优先最小改动
优先使用最小改动修复问题，不做无必要重构。

---

## 12. 当前版本完成标准

当以下条件全部满足时，视为当前 V1 完成：

- Gazebo 中可看到机器人
- 机器人可响应运动控制
- `/scan` 稳定输出
- TF 正常
- `slam_toolbox` 能正常建图
- 有一版 `README`
- 有本 `design.md`
- 能整理成简历中的副项目描述

---

## 13. 后续版本规划

### V1
AMR 仿真建图最小闭环

### V2
导航 / 路径执行扩展

可考虑：

- Nav2
- 路径控制
- 点到点移动
- CAD 地图利用

### V3
系统级扩展

可考虑：

- WMS 对接
- 任务下发
- 状态回传
- 多模块协同验证

---

## 14. 每日记录模板

建议每天补充：

### 今日目标
### 实际完成
### 当前卡点
### 明日第一步

示例：

- 今日目标：确认机器人是否真正被 Gazebo 加载
- 实际完成：确认 `/scan` 正常，发现实体树中没有 robot
- 当前卡点：可能是 world include 或模型路径问题
- 明日第一步：检查 world / launch 中模型引用与资源路径

### 2026-04-24 记录

- 今日目标：打通 V1 建图链路，并确认小车可见、可动、可输出地图。
- 实际完成：机器人由 world 直接加载；`/scan`、`/odom`、TF、`/map` 已打通；`slam_toolbox` 改为 launch 自动激活；RViz odometry 话题改为 `/odom`。
- 发现问题：旧 `ros2 topic pub -r ... /cmd_vel` 会抢占键盘控制；旧 `nav2_slam.launch.py` 和旧 TF 进程会污染当前主线；地图质量仍受驾驶路线影响。
- 明日第一步：干净启动后，只做小范围慢速闭环建图，确认局部地图稳定后再扩展到仓库通道。
